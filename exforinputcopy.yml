---
platform: linux
image_resource:
  type: docker-image
  source: {repository: ubuntu}

inputs:
- name : resources
  path: input-resources-folder


outputs:
- name: output-folder

run:
  path: ./input-resources-folder/scriptfile.sh